name: NBA V2 Feature Exhaustive Search

on:
  workflow_dispatch: 

jobs:
  run_search:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Check out repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy requests bs4 urllib3 tqdm nba_api catboost scikit-learn

    # 🚀 執行全新的 V2 實驗腳本！
    - name: Run V2 Exhaustive Search (Takes 30-50 mins)
      env:
        PROXY_URL: ${{ secrets.PROXY_URL }}
        PYTHONPATH: src
      run: python src/exhaustive_search_v2.py 

    # 💾 把 V2 產生的排行榜 CSV 存回 GitHub
    - name: Commit and Push Ranking Report
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        
        # 只推送 V2 產生的新報告
        git add exhaustive_search_v2_results.csv || true
        
        git commit -m "🔬 [Experiment] V2 Exhaustive Feature Search Completed" || echo "No changes to commit"
        git push
