name: NBA V2 Daily Backtest Test

on:
  workflow_dispatch: 

jobs:
  run_search:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Check out repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy requests bs4 urllib3 tqdm nba_api catboost scikit-learn

    # 🚀 執行全新的 V2 模型隔離測試
    - name: Run V2 Daily Backtest
      env:
        PROXY_URL: ${{ secrets.PROXY_URL }}
        PYTHONPATH: src
      run: python src/v2_daily_backtest_test.py 

    # 💾 把跑出來的 V2 測試排行榜 CSV 存回 GitHub
    - name: Commit and Push Report
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        
        # 只推送這份隔離測試報告
        git add v2_daily_backtest_results.csv || true
        
        git commit -m "🔬 [Experiment] V2 Daily Backtest Completed" || echo "No changes to commit"
        git push
