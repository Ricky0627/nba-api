name: NBA Daily MLOps Pipeline

on:
  workflow_dispatch: # 手動觸發

jobs:
  daily_update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Check out repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy requests bs4 urllib3 tqdm nba_api catboost scikit-learn

    # 🚀 除錯模式：跳過爬蟲，只單獨執行 generate_injury.py
    - name: Run generate_injury.py ONLY
      env:
        # 告訴 Python 也要去 src 資料夾裡面找模組 (解決找不到 prepare_data 的問題)
        PYTHONPATH: src 
      run: python src/generate_injury.py 

    # 💾 只上傳這個特徵檔案，看看有沒有成功產出
    - name: Commit and Push Results
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        
        # 只把這個檔案加入追蹤
        git add nba_advanced_injury_features.csv || true
        
        git commit -m "🤖 [Debug] Test generate_injury.py only" || echo "No changes to commit"
        git push
