name: NBA Top 10 Daily Backtest

on:
  workflow_dispatch: 

jobs:
  run_search:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Check out repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy requests bs4 urllib3 tqdm nba_api catboost scikit-learn

    # 🚀 執行我們剛寫好的十大模型逐日回測
    - name: Run Top 10 Daily Backtest
      env:
        PROXY_URL: ${{ secrets.PROXY_URL }}
        PYTHONPATH: src
      run: python src/top10_daily_backtest.py 

    # 💾 把跑出來的排行榜 CSV 存回 GitHub
    - name: Commit and Push Ranking Report
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        
        # 只推送這個報告檔案
        git add top10_daily_backtest_results.csv || true
        
        git commit -m "🔬 [Experiment] Top 10 Daily Backtest Completed" || echo "No changes to commit"
        git push
