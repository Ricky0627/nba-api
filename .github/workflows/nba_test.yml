name: NBA Feature Exhaustive Search

on:
  workflow_dispatch: 
    # 只能手動觸發，跑完就會產生 CSV 報告

jobs:
  run_search:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Check out repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy requests bs4 urllib3 tqdm nba_api catboost scikit-learn

    # 🚀 執行 Python 腳本 (這裡才是呼叫你剛剛寫的 Python 檔的地方)
    - name: Run Exhaustive Search (Takes 30-60 mins)
      env:
        PROXY_URL: ${{ secrets.PROXY_URL }}
        PYTHONPATH: src
      run: python src/exhaustive_search.py 

    # 💾 把跑出來的排行榜 CSV 存回 GitHub
    - name: Commit and Push Ranking Report
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        
        # 只推送這個排行榜檔案
        git add exhaustive_search_results.csv || true
        
        git commit -m "🔬 [Experiment] Exhaustive Feature Search Completed" || echo "No changes to commit"
        git push
