name: NBA Daily MLOps Pipeline

on:
  schedule:
    # 每天早上 10 點 (台灣時間) 自動跑一次 (UTC 2:00)
    - cron: '0 7 * * *'
  workflow_dispatch: 
    # 允許手動從網頁按鈕觸發

jobs:
  daily_update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Check out repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy requests bs4 urllib3 tqdm nba_api catboost scikit-learn

    # 🚀 執行完整的自動化管線 (包含所有爬蟲、特徵工程與模型訓練)
    - name: Run Entire NBA Pipeline
      env:
        PROXY_URL: ${{ secrets.PROXY_URL }}
        PYTHONPATH: src
      run: python src/run_pipeline.py 

    # 💾 把跑完產生的所有新資料、特徵與模型存回 GitHub
    - name: Commit and Push Results
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        
        # 加入所有會更新的檔案 (加上 || true 避免檔案沒變動時報錯)
        git add data/nba_current.db || true
        git add nba_advanced_injury_features.csv || true
        git add nba_daily_walkforward_predictions.csv || true
        git add nba_daily_walkforward_summary.csv || true
        git add models/ || true
        
        git commit -m "🤖 [MLOps] Daily Auto-Update: Data, Features & Models" || echo "No changes to commit"
        git push
