name: NBA Random Forest Backtest

on:
  workflow_dispatch: 

jobs:
  run_search:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Check out repository
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy requests bs4 urllib3 tqdm nba_api catboost scikit-learn

    # 🚀 啟動 Scikit-Learn Random Forest 隔離測試
    - name: Run RF Daily Backtest
      env:
        PROXY_URL: ${{ secrets.PROXY_URL }}
        PYTHONPATH: src
      run: python src/rf_daily_backtest_test.py 

    # 💾 儲存 RF 測試報告
    - name: Commit and Push Report
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "github-actions[bot]"
        
        git add rf_daily_backtest_results.csv || true
        
        git commit -m "🔬 [Experiment] Random Forest Daily Backtest Completed" || echo "No changes to commit"
        git push
